{
  "name": "care-diagnostics-fingerprint-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "Local USB fingerprint scanner bridge for Care Diagnostics ERP. Runs on each workstation, talks to vendor SDK (ZKTeco/Mantra/Morpho/...) and forwards templates to the ERP API.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^5.0.0",
    "cors": "^2.8.5"
  }
}
